// Jenkinsfile — for ci.jenkins.io plugin builds
buildPlugin(
  forkCount: '1C',
  useContainerAgent: true,
  configurations: [
    [platform: 'linux',   jdk: '21',  jenkins: null],
    [platform: 'windows', jdk: '17',  jenkins: null],
  ]
)